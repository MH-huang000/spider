3. 在主控台输入

// 获取所有符合条件的text标签

var textElements = document .querySelectorAll('text[fill="#cfcfcf"][stroke="none"]');

/1/ 删除符合条件的text标签

textElements.forEach(function(element) {
element . remove();

Ds
